<UserControl x:Class="ArcGISProLayerInspector.LayerInfoDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </UserControl.Resources>
    <DockPanel Margin="8">
        <!-- Top: Language + Layer selection -->
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="{Binding LblTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

            <DockPanel Margin="0,0,0,6">
                <TextBlock Text="{Binding LblLanguage}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding AvailableLanguages}" 
                          SelectedItem="{Binding SelectedLanguage}"/>
            </DockPanel>

            <Button Content="{Binding LblRefreshLayers}" 
                    Command="{Binding LoadLayersCommand}" 
                    Margin="0,0,0,6" Padding="6,3"/>
            <TextBlock Text="{Binding LblSelectLayer}" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding Layers}" 
                      SelectedItem="{Binding SelectedLayer}" 
                      DisplayMemberPath="Name"
                      Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Bottom: Layer details -->
        <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
        <Border BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="0,8,0,0"
                Visibility="{Binding HasInfo, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="{Binding LblLayerDetails}" FontSize="14" FontWeight="Bold" Margin="0,0,0,6"/>

                <TextBlock Text="{Binding LblName}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding LayerName}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblShapeType}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding ShapeType}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblSpatialReference}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding SpatialReference}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblFeatureCount}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding FeatureCount}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblMaxExtent}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding MaxExtent}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblDataSource}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding DataSource}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblHasZ}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding HasZ}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblHasM}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding HasM}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblVisible}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding IsVisible}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblSelectable}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding IsSelectable}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblEditable}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding IsEditable}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblMinScale}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding MinScale}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblMaxScale}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding MaxScale}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Text="{Binding LblDefinitionQuery}" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="{Binding DefinitionQuery}" Style="{StaticResource ValueStyle}"/>

                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="{Binding LblFieldCount, Mode=OneWay}"/><Run Text="{Binding FieldCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Text="{Binding FieldList}" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
        </Border>
        </ScrollViewer>
    </DockPanel>
</UserControl>
